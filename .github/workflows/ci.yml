name: CI

on:
  push: ~
  pull_request:
    branches:
      - master

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Build
        run: composer install
      - name: Tests
        run: ./vendor/bin/phpunit